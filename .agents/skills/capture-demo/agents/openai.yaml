interface:
  display_name: "Capture Demo"
  short_description: "Capture browser and isolated terminal demos as GIFs"
  default_prompt: "Use $capture-demo to record the requested browser flow or isolated terminal session and deliver a verified animated GIF."

policy:
  allow_implicit_invocation: false
